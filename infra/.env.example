# Azure SQL Server Administrator Password
# This password is used for:
# 1. Creating SQL Servers during deployment (deploy.sh)  
# 2. Connecting to databases for data import/operations
SQL_ADMIN_PASSWORD=your-secure-password-here

# Database connection parameters for CSV import
# Copy this file to .env and fill in your actual values
# Note: DB_PASSWORD should match SQL_ADMIN_PASSWORD

# Azure SQL Server connection details
DB_SERVER=your-server-name.database.windows.net
DB_DATABASE=SystemDemoDB
DB_USERNAME=your-username
DB_PASSWORD=${SQL_ADMIN_PASSWORD}
DB_PORT=1433

# Optional: Override driver if needed
# DB_DRIVER={ODBC Driver 18 for SQL Server}

# === COSMOS DB CONNECTION (Auto-populated by deploy.sh) ===
# Cosmos DB details for manufacturing defects data
COSMOS_ACCOUNT=your-cosmos-account-name
COSMOS_ENDPOINT=https://your-cosmos-account.documents.azure.com:443/
COSMOS_DATABASE=your-cosmos-database
COSMOS_PRIMARY_KEY=your-cosmos-primary-key
COSMOS_CONNECTION_STRING=AccountEndpoint=https://your-cosmos-account.documents.azure.com:443/;AccountKey=your-key;Database=your-db;

# Password Requirements for SQL_ADMIN_PASSWORD:
# - At least 8 characters
# - Must contain characters from at least 3 of the following 4 categories:
#   - Uppercase letters (A-Z)
#   - Lowercase letters (a-z)  
#   - Numbers (0-9)
#   - Special characters (!@#$%^&*()_+=-[]{}|;:,.<>?)
